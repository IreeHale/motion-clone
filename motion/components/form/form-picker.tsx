"use client"

interface FormPickerProps {
    id: string;
    errors?: Record<string, string[] | undefined>;
};


export const FormPicker = () => {
    return (
        <div>
            Form Picker!
        </div>
    );
};